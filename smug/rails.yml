session: ${SESSION}
root: .

windows:
  - name: Code
    selected: true
    commands:
      - bundle
      - yarn
  - name: Server
    panes:
      - type: horizontal
  - name: Routes
  - name: Production
