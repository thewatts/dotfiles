session: ${SESSION}
root: ${ROOT}

windows:
  - name: Code
    selected: true
    commands:
      - bundle
      - yarn
  - name: Server
    commands:
      - bin/rails db:snapshot:restore
      - bin/rails s
  - name: Assets
    commands:
      - a
  - name: Spring
    commands:
      - ss
  - name: Routes
    commands:
      - routes
  - name: Production
